import type { ReactNode } from "react";

export const metadata = { title: "Integrations setup" };

export default function IntegrationsLayout({ children }: { children: ReactNode }) {
  return children;
}
